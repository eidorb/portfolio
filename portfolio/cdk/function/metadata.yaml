title: Portfolio
description_html: View dashboard <a href="/-/dashboards/portfolio">here</a>.
plugins:
  datasette-dashboards:
    portfolio:
      title: Portfolio
      filters: {}
      charts:
        value:
          title: Asset value (AUD) over time
          db: portfolio
          query: |
            select
              date,
              asset as Asset,
              round(sum(value_number)) as value
            from
              normalised_balance_aud
            where
              account not like 'Liabilities:StateCustodians:%'
            group by
              date,
              Asset
          library: vega-lite
          display:
            mark:
              type: area
              tooltip: true
            encoding:
              x:
                timeUnit: yearmonthdate
                field: date
                title: Date
              y:
                aggregate: sum
                field: value
                title: Total value (AUD)
              color:
                field: Asset
                scale:
                  scheme: pastel1
        total:
          title: Total portfolio value
          db: portfolio
          query: select printf('AUD $%,d', sum(amount_aud)) as total_aud from asset_classes
          library: metric
          display:
            field: total_aud
        change:
          title: Percentage change required to reach target allocations
          db: portfolio
          query: select * from changes order by change desc
          library: vega-lite
          display:
            encoding:
              x:
                field: change
                type: quantitative
                stack: null
                axis: null
              y:
                field: asset_class
                type: nominal
                sort: null
                axis: null
              color:
                value: red
                condition:
                  test: datum.change>0
                  value: limegreen
            layer:
              - mark: bar
              - mark:
                  type: text
                  align:
                    expr: datum.change>0?'right':'left'
                  dx:
                    expr: datum.change>0?-4:4
                encoding:
                  text:
                    field: asset_class
                  color:
                    value: black
                  x:
                    datum: 0
              - mark:
                  type: text
                  align:
                    expr: datum.change>0?'left':'right'
                  dx:
                    expr: datum.change>0?4:-4
                encoding:
                  text:
                    field: change
                    format: .1%
                  color:
                    value: black
        steps:
          title: Steps to rebalance
          db: portfolio
          query: |
            select
              case
                when action == 'Buy' then '<span style="color: limegreen">Buy</span>'
                else '<span style="color: red">Sell</span>'
              end as '<strong>Action</strong>',
              printf('%s %s', amount, asset) as '<strong>Asset</strong>',
              amount_aud as '<strong>Value (AUD)</strong>',
              portfolio_percentage as '<strong>Percentage of portfolio</strong>'
            from
              actions
          library: table
